require 'spec_helper'

describe DatabaseTransform do
  it 'has a version number' do
    expect(DatabaseTransform::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
